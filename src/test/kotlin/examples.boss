#paket "hello.boss";

var nesto = "str"
konst pi = 3.14

za svako (var x od 0 do 10 korak 2){
    ispis(x)
}

var brojevi = [12, 31, 1, 18, 1, 93]

za svako (var x u brojevi){
    ispis(x)
}

za svako (var x od 0 do duzina(brojevi)){
    ispis(brojevi[x])
}

konst naslov = dokument.pronadjiElement(".naslov")
naslov.tekst = nesto

objekat obj = {
    ime: "Primjer",
    vrijednost: 5,
    dug: netacno
}

konst objekat kobj = {
    port: 8080
}

klasa x < H1HTMLElement{
    javna var ime<tekst>
    javna var klasa<tekst>
    privatna var atributi<niz<tekst>>
    konstruktor(){}
    privatna funkcija racun(){}
    javna staticna funkcija primjer(){}
}

var x = 0
dok(x != 10){
    ispis(x)
    x++
}